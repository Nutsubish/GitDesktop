// function manualMap(func, iterable) {
//     return iterable.map(func);
//   }

//   function manualFilter(func, iterable) {
//     return iterable.filter(func);
//   }  

//   function isLongName(name) {
//     return name.length > 5;
//   }
  
//   let names = ["Christopher", "Emma", "Michael", "Sam"];
//   let shortNames = manualFilter((name), function(isLongName,name)
//       return name
// );
//   console.log(shortNames); 